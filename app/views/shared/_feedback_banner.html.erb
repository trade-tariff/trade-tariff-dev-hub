<% unless @feedback %>
  <div class="tariff-feedback-banner">
    <%= govuk_phase_banner(tag: { text: local_assigns.fetch(:tag_text, 'FEEDBACK') }) do %>
      Tell us what you think - your <%= govuk_link_to 'feedback', TradeTariffDevHub.feedback_url %> will help us improve.
    <% end %>
  </div>
<% end %>
